========================================================================
PHOTOTOOL - PROJECT INSTRUCTIONS & DECISIONS LOG
========================================================================
Last updated: 2026-06-25
Purpose of this file: a single place that captures everything we have
agreed on, so the project can be picked up again at any time (by you or
by Claude in a fresh conversation). Read this top-to-bottom to get back
up to speed.

------------------------------------------------------------------------
1. WHAT WE ARE BUILDING (current scope)
------------------------------------------------------------------------
A mobile-friendly WEBSITE (not a native app) for collecting photos from
guests' phones.

User flow:
  1. A guest scans a QR code.
  2. The QR opens the website in their phone browser (works on iOS and
     Android).
  3. The page shows a big primary button in POLISH that opens the
     phone's native photo library / picker.
  4. The guest selects one or more photos.
  5. Selected photos are shown as thumbnail previews on the page.
  6. A SEND button is ALWAYS visible, fixed to the bottom of the screen.
  7. Tapping SEND uploads the selected photos (to a backend we add
     later) with a progress indicator and a success confirmation.

This pattern is commonly used for weddings / parties / events where many
guests contribute photos to one shared collection.

------------------------------------------------------------------------
2. KEY DECISIONS AGREED SO FAR
------------------------------------------------------------------------
- PLATFORM: Web page (mobile browser), not a native iOS/Android app.
  Works on both iOS and Android because it's just a website.
- ACCESS: Via QR code that points to the website URL.
- USERS: Anonymous guests. No login, no account, no name required.
- UI STYLE: Minimal & functional. Clean, just the essentials:
  upload button, thumbnail previews, sticky SEND button, progress.
  No branding/logo for now.
- LANGUAGE: Polish UI text.
- WORDING CONFIRMED:
    * Top/upload button:  "PRZEŚLIJ ZDJĘCIA"
    * Send button:        "WYŚLIJ"
- MEDIA TYPES: Images AND videos accepted.
- LIMITS: None for now. FUTURE TOPIC (revisit): client-side compression
  for very large files (example threshold ~300 MB) before upload.
- HOSTING (for now): GitHub Pages (github.io) - static hosting only.
    * Repo:     https://github.com/eldharon/WeseleChrupakow
    * Account:  eldharon
    * Visibility: PUBLIC (required for free GitHub Pages)
    * Live URL: https://eldharon.github.io/WeseleChrupakow/
    * Branch/path served: main, root (/)
    * Note: repo name was requested as "WeseleChrupaków" but GitHub
      disallows non-ASCII; used "WeseleChrupakow". Display title inside
      the app can still be styled/Polish separately.
- STORAGE (CHOSEN): a Google DRIVE folder owned by the user.
    * NOT Google Photos - its API was restricted in 2025 and cannot take
      anonymous guest uploads into the owner's library. Drive folder is
      the practical target.
    * Mechanism: a Google APPS SCRIPT web app (file google-apps-script.gs)
      acts as the backend. It runs AS the owner, so it may write to the
      owner's Drive; it is deployed with "Anyone" access so guests need no
      login. The website POSTs each file (base64) to the script URL; the
      script saves it into the Drive folder. No credential/token is ever
      placed in the public website.
    * Connection point: CONFIG.WEB_APP_URL in app.js (currently EMPTY -
      must be filled with the deployed /exec URL).
- BACKEND: Google Apps Script web app (see google-apps-script.gs). The
  on-prem server is still a possible FUTURE alternative; if/when built,
  just point CONFIG.WEB_APP_URL (or uploadFiles) at it instead.

  KNOWN LIMITS of the Apps Script route:
    * Good for photos and short videos. Each file is sent base64 in one
      request; Apps Script caps payloads (~50 MB), so LARGE VIDEOS will
      fail. This is exactly why "compress large files" stays a future
      topic, and/or large media waits for the on-prem server.
    * Anyone with the script URL can POST to it (no auth yet). Fine for a
      trusted event; can add a shared secret later if needed.
    * If reading the response is blocked by CORS in practice, fall back to
      fetch(..., {mode:"no-cors"}) fire-and-forget (can't read result).

------------------------------------------------------------------------
3. IMPORTANT TECHNICAL CONSTRAINT (read this)
------------------------------------------------------------------------
GitHub Pages can HOST the website, but it CANNOT STORE uploaded files.
  - GitHub Pages is static hosting: it only serves HTML/CSS/JS.
  - It has no server-side code, so it cannot receive or save uploads.
  - A git repo is not writable by website visitors, and committing
    guests' photos into a (public) repo would be wrong anyway.

Therefore, in the current phase:
  - The website is fully built and testable.
  - The SEND button is wired to a CLEARLY-MARKED PLACEHOLDER upload
    function (a single, isolated function in the code).
  - When the real server exists, we change only that one function.

What "works" now vs. later:
  - WORKS NOW: QR -> page loads -> Polish upload button -> native photo
    picker -> multi-select -> thumbnail previews -> sticky SEND button
    -> progress UI -> success/error messages.
  - NOT YET: photos actually being stored somewhere. That needs the
    backend (section 4).

------------------------------------------------------------------------
4. PLANNED NEXT PHASE - ON-PREM SERVER (to do later)
------------------------------------------------------------------------
You will set up an on-prem (home) server later and ask Claude to "plug
it in." When that happens, we will need to decide/answer:

  - Server type/OS (NAS, Raspberry Pi, old PC, Linux/Windows, Docker?).
  - Public reachability: phones uploading over cellular need the server
    reachable from the internet (port forward + dynamic DNS, a tunnel
    like Cloudflare Tunnel/Tailscale Funnel, or a reverse proxy).
  - HTTPS certificate (browsers + camera/file APIs strongly prefer
    HTTPS; mixed content from an https page to http server will fail).
  - The upload endpoint contract (e.g. POST multipart/form-data to
    /upload, returns 200 on success).
  - Where files land on disk and how they're named/organized
    (e.g. one folder, or by date, to avoid filename collisions).
  - CORS: the server must allow uploads from the GitHub Pages origin.
  - Basic abuse protection (see section 5).

Integration point in the code: the single placeholder upload function.
Only its body (the actual network call to the server) needs to change.

------------------------------------------------------------------------
5. OPEN QUESTIONS / NOT YET DECIDED
------------------------------------------------------------------------
- LARGE-FILE COMPRESSION (FUTURE TOPIC - agreed to revisit): compress
  files above some threshold (example: ~300 MB) before uploading, to
  reduce upload time/storage. Not implemented in v1. Notes for later:
    * Images: can be downscaled/re-encoded client-side via canvas.
    * Videos: real client-side compression is heavy (needs something
      like ffmpeg.wasm) and slow on phones; may be better handled
      server-side once the on-prem server exists. Decide approach then.
- PASSWORD / ACCESS PROTECTION: none for now (fully open). Optional
  later.
- QR CODE: we still need to generate it once the final URL is known
  (the github.io URL). Trivial to produce from the URL.

------------------------------------------------------------------------
6. TECH PLAN (proposed - not yet built)
------------------------------------------------------------------------
- Plain static site: index.html + styles + a small script. No framework
  needed for v1 (keeps it simple and GitHub-Pages-friendly). Could use a
  framework later if desired.
- Native picker via:  <input type="file" accept="image/*" multiple>
  (Optionally capture from camera too; multi-select from library is the
  priority.)
- Thumbnail previews via object URLs in the browser.
- Sticky SEND button via fixed/sticky CSS positioning at viewport bottom.
- Upload via fetch() multipart/form-data inside the ONE placeholder
  function (currently stubbed/simulated).
- Deploy: push to a GitHub repo, enable GitHub Pages -> get the
  https://<user>.github.io/<repo>/ URL -> make the QR from that URL.

------------------------------------------------------------------------
7. STATUS
------------------------------------------------------------------------
[x] Frontend page built  (index.html, styles.css, app.js)
[x] Hosted on GitHub Pages
[ ] QR code generated from final URL
[x] Polish wording confirmed  (PRZEŚLIJ ZDJĘCIA / WYŚLIJ)
[x] Limits/protection decided  (none for v1; compression is a future topic)
[x] Storage target chosen: Google Drive folder via Apps Script
[x] Apps Script backend written (google-apps-script.gs)
[x] Website wired to upload to Apps Script (real uploadFiles in app.js)
[ ] User deploys the Apps Script & provides the /exec URL
[ ] CONFIG.WEB_APP_URL filled in app.js + pushed
[ ] End-to-end test (guest phone -> Drive folder)
[ ] On-prem server (optional future alternative)

Currently: code is ready to upload to Google Drive, but it is NOT live
until the user deploys google-apps-script.gs and we paste the resulting
/exec URL into CONFIG.WEB_APP_URL in app.js. Until then, pressing SEND
shows a "not configured" error on purpose.

TO FINISH (user's part, in Google account):
  Follow the SETUP steps at the top of google-apps-script.gs:
  create a Drive folder, paste its ID into FOLDER_ID, deploy as a Web app
  (Execute as: Me / Who has access: Anyone), then hand the /exec URL back.
THEN (Claude's part):
  Put that URL in CONFIG.WEB_APP_URL, push, and test end-to-end.

------------------------------------------------------------------------
8. HOW TO RESUME THIS PROJECT
------------------------------------------------------------------------
Open a new conversation and say: "Read instructions.txt in the PhotoTool
folder and continue." Claude should read this file first, then proceed
from the STATUS section.
========================================================================
